import { TitleStylingDirective } from './title-styling.directive';

describe('TitleStylingDirective', () => {
  it('should create an instance', () => {
    const directive = new TitleStylingDirective();
    expect(directive).toBeTruthy();
  });
});
